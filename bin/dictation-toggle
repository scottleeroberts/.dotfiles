#!/usr/bin/env bash

STATE_FILE="/tmp/nerd-dictation.state"

if [[ -f "${STATE_FILE}" ]]; then
    # Already recording, stop it
    /home/scott.roberts/.dotfiles/bin/dictation-stop
else
    # Not recording, start it
    /home/scott.roberts/.dotfiles/bin/dictation-start
fi
